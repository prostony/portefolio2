//function Ajout de classe pour gerer le menu
function AjoutClass() {
  var x = document.getElementById("myTopnav");
  if (x.className === "topnav") {
    x.className += " responsive";
  } else {
    x.className = "topnav";
  }
}

let date = new Date();
let annee = date.getFullYear();
let element = document.getElementById('current_year');
element.textContent = annee;
/**
 * ce script permet de recuperer la valeur saisie dzns le champ textarea et de la placer comme corps du mail lors de l'envoir 
 * si une fonction se trouve dans les parentheses e.g : (function() {}) elle est isolée et donc tous ses ééments ne peuvent être utilisées qu'à l'intérieur
 */

/**
 * // Récupérer le contenu du champ de saisie
var bodyText = document.getElementById("myTextarea").value;

// Créer le lien mailto avec le corps du message
var mailtoLink = "mailto:email@example.com?body=" + encodeURIComponent(bodyText);

// Rediriger l'utilisateur vers le client de messagerie par défaut
window.location.href = mailtoLink;
 */
/*parcontre la fonction fléché elle doit etre stockée dans une variable sinon elle ne fonctionne pas */
/*const form = document.querySelector('form');
  const message = document.querySelector('#message');
  form.addEventListener('submit', (event) => /*ici la flèche remplace le mot spécifique function du coup c'est autre façon de declarer une fonction en JS*//*{
    event.preventDefault();
    const body = encodeURIComponent(message.value);

    const mailtoLink = `mailto:nathan.stewill.loufoua@diderot-campus.fr?cc=secondemail@example.com&bcc=lastemail@example.com&subject=Mail from our Website&body=${body}`;

    // Rediriger l'utilisateur vers le client de messagerie par défaut
    window.location.href = mailtoLink;
  });*/